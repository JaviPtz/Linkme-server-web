// const mongoose = require("mongoose");
// const workSchema = require("../models/work.model");

// workSchema.statics = {
//   listWorks: function (query) {
//     this.find(query);
//   },
//   createWork: function (data, cb) {
//     const work = new this(data);
//     work.save(cb);
//   },
// };

// const worksModel = mongoose.model("Works", workSchema);
// module.exports = worksModel;
